{
  "compilerOptions": {
    /* -------------- 基本設定 -------------- */
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Node",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "jsx": "react-jsx",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "useDefineForClassFields": true,
    "allowJs": true,
    "isolatedModules": true,
    "noEmit": true,

    /* -------------- 型チェック緩和 -------------- */
    "strict": false,
    "noImplicitAny": false,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* -------------- パス設定 -------------- */
    "baseUrl": "./src",        // 以後 import を src 基準に解決
    "rootDir": "./src",        // これより外の TS は原則コンパイルしない
    "paths": {
      "@/*": ["*"]             // 例） import x from "@/pages/Top"
    },

    /* -------------- その他 -------------- */
    "types": ["node", "@testing-library/jest-dom"]
  },

  /* src 直下だけを対象にする */
  "include": ["src/**/*"],

  /* それでも拾われたら完全無視させる */
  "exclude": [
    "node_modules",
    "demos/**",
    "services/**",
    "src/services/**",
    "tests/**",
    "src/tests/**"
  ]
}
